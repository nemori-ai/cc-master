# Track B without-skill run 3

- endpoint: Claude Code 2.1.206 / `sonnet` / high effort
- isolation: fresh, no persistence, safe mode, read-only `Read` only; prompt forbade skill/methodology reads
- session: `7b548617-7a2f-4006-9610-00cc6e0e0f5c`

## Final response

# 合并就绪备忘录：Lumen 适配器（Track B）

## 结论：**不可合并（Not Ready）**

“28 个映射面全绿”这一数字具有误导性——它衡量的是 *源/dist 一致性*，而非 *语义正确性*。证据显示至少 3 个映射面存在行为降级，且引入了一处文档权威性矛盾与一处技术债（skill 内容分叉）。25/28 是真行为对等，其余 3 个是伪装成对等的近似实现，测试套件并未覆盖这个差异。

## 阻塞项（按严重程度排序）

1. **`beforeCondense`：能力缺失，非近似**
   Lumen 只能观察/记录，无法在压缩后注入恢复上下文。现有实现依赖注入才能恢复状态。这不是“最接近的实现”，而是功能空集——必须在文档和运行时都显式标注为不支持，而不是悄悄复用别处代码造成“看似支持”的假象。

2. **`stop`：静默降级为建议**
   现有实现是硬阻断以保证 board state 安全；Lumen 只能附加一条供后续轮次参考的建议，且无法阻止本轮完成。如果这个语义差异没有在调用方/文档中显式暴露，会导致 board state 不安全的完成被静默放行——这是正确性问题，不是风格问题。

3. **批处理是猜测出来的行为，不是移植的行为**
   Lumen 没有原生批完成事件，补丁“复制最接近实现并择机 flush”。这是一种启发式近似，尚无证据证明其时序/原子性与现有主机等价，且没有说明这一点在测试中如何被验证。

4. **文档权威性矛盾，未解决**
   `compatibility-matrix.md` 说已发布，`lumen.md` 说是未来草案。两份文档口径不一致本身就应该阻止合并——发布状态是决定下游是否可以依赖该适配器的关键信号，不能留待事后澄清。

5. **Skill 内容分叉**
   为改两个路径示例，直接把权威 skill 文案复制进 Lumen 专属目录。这创建了第二个真源，后续任何 skill 更新都会漏掉这个副本，是可预见的维护债务。

## 有效证据（不必阻塞）

25 个真正行为对等的映射面，可以合并进一个明确标注为“部分 / 已知降级”的版本。

## 排序后的行动项

1. 立即撤回“28 个映射面全绿”的表述，改为“25/28 行为对等，3 个已知降级”。
2. 解决 `compatibility-matrix.md` 与 `lumen.md` 的权威性冲突（二选一，加一句说明为什么）。
3. 为 `beforeCondense`、`stop`、批处理这三处分别写明确的能力限制声明，而不是用近似代码掩盖。
4. 撤销对 skill 文案的直接复制，改为引用共享源或参数化路径示例。
5. 补充针对这 3 个降级面的专项测试（当前的“direct-mapping tests”不能证明其正确性）。

**35 分钟内不具备安全合并这份补丁的条件**；建议按上述顺序修复文档与降级声明后再评审，代码逻辑本身可以保留。
