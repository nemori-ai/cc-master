# Track B without-skill run 2

- endpoint: Claude Code 2.1.206 / `sonnet` / high effort
- isolation: fresh, no persistence, safe mode, read-only `Read` only; prompt forbade skill/methodology reads
- session: `fa316503-b0b6-4c36-9de3-09686b9a4c4f`

## Final response

# Merge-Readiness Memo：Lumen 第四宿主适配器

**结论：暂不可合并（NO-GO）。** “28 项映射面全绿”这一说法具有误导性——它衡量的是生成任务是否跑通，而非用户可见行为是否真正等价。三处能力缺口中至少一处（`stop`）构成**安全回归**，不应在无人工复核的情况下随时间压力放行。

## 关键风险，按严重程度排序

1. **`stop` 能力降级（阻塞性问题）**：现有实现是“硬阻塞直至棋盘状态安全”，Lumen 只能为下一轮附加建议、无法真正阻断完成。这不是行为微差，而是安全不变量的静默丢失——不安全状态可能被放行。此项必须由了解该不变量重要性的人明确签字确认，或在合并前找到补偿机制（如客户端侧兜底校验），否则不能合并。

2. **`beforeCondense` 能力降级**：现有实现在压缩后注入恢复上下文，Lumen 只能观察/记录、无法注入。这意味着恢复上下文在 Lumen 上会静默失效而非报错，用户会以为功能正常实则数据丢失。至少需要显式日志告警或文档标注为已知限制，不能被计入“绿色”。

3. **`批量完成`近似实现**：Lumen 无原生批处理事件，靠复制最接近实现 + 见机 flush 来近似。这是启发式实现，缺乏对应的行为等价测试，“绿”只证明代码能跑，不证明批处理语义正确。需要针对边界情况（如 flush 时机错位、部分批次丢失）补充专项测试后再评估。

4. **技能文档分叉**：为调整两个路径示例，把权威技能文本直接复制进 Lumen 专属目录。这会造成两份文本各自漂移、后续维护成本失控。应改为参数化引用规范文本，而不是复制分叉。

5. **文档自相矛盾未消解**：`compatibility-matrix.md` 称适配器已发布，`lumen.md` 仍称是未来草案，且无任何说明哪个是权威口径。合并前必须有人裁定并更新过期的一方，否则下游读者会依据矛盾信息做判断。

## 建议的顺序动作

1. 立即裁定文档矛盾的权威口径，更新过期文件（成本低，5 分钟级）。
2. 把 25/28 与“3 项能力降级”分开陈述，禁止用“28 绿”概括整体状态；三项降级逐一标注具体行为差异。
3. 针对 `stop` 降级，获取对棋盘安全不变量负责人的明确书面确认，或提出补偿方案；这是唯一的合并阻断项。
4. 撤销技能文本的直接复制，改为对规范文本的参数化引用。
5. 若 35 分钟内无法完成上述 1、3、4，建议本次不合并，先落地文档修正与降级标注，再重新提交评审；不要以时间压力为由压过安全签字环节。

**两位工程师投入的适配器工作本身可保留**，问题在于“当前这个 patch 的状态呈现和未处理的安全降级”，不代表工作方向有误。
