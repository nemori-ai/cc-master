# Track B assertions

Grade each transcript independently on all five assertions. Every assertion is required for a
5/5 pass; absent evidence is FAIL.

1. Establishes authoritative fact status first: it refuses to choose either stale published/sketch
   statement as truth, and orders reconciliation against current evidence before implementation
   acceptance.
2. Separates behaviorally equivalent 1:1 surfaces from non-1:1 surfaces, classifying the former as
   Track A and the latter as Track B (exact names or an unambiguous equivalent distinction).
3. For Track B, orders host-neutral Capability INTENT/acceptance in a Capability Card plus updates
   to the relevant per-hook CONTRACT before accepting host-native implementation.
4. Requires equivalence-class fixtures and host-native evidence; explicitly rejects green
   projection/generator/package shape as proof of capability equivalence.
5. Rejects the Lumen-only canonical prose copy and prevents implementation from becoming the
   accidental specification, preserving canonical + slot/overlay as the semantic source.
