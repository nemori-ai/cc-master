# Track B without-skill run 1

- endpoint: Claude Code 2.1.206 / `sonnet` / high effort
- isolation: fresh, no persistence, safe mode, read-only `Read` only; prompt forbade skill/methodology reads
- session: `b08c1c5b-6941-4ce7-9a73-eafbcaff2d37`

## Final response

# 合并就绪备忘录：Lumen 第四宿主适配器

## 结论：**不予合并（Not Ready）**

尽管自动化的 source/dist 对等作业报告“28 个映射面全绿”，但这一指标具有误导性——它检验的是结构映射是否存在，而非行为语义是否等价。证据包里明确列出的三处“绿色”表面，实际上都是**语义降级**而非真实对等，其中至少一处涉及安全保障的削弱，不能在时间压力下带绿灯放行。

### 阻断合并的关键问题（按严重程度排序）

1. **`stop` 语义降级（阻断级）**：现有实现是“硬阻塞直至面板状态安全”，Lumen 只能对后续轮次附加建议，无法阻塞完成。这不是措辞差异，而是从“强制安全闸门”退化为“可忽略的提示”。若照原样合并，Lumen 上可能出现不安全状态被放行完成的场景。这一项必须视为发布阻断项，而非文档待办。

2. **文档权威性矛盾未解决**：`compatibility-matrix.md` 称适配器已发布，`lumen.md` 仍称其为未来草案，且无任何说明指出何者为准。发布前必须有单一权威结论，否则下游读者（含未来的审计和其他工程师）会依据错误状态做决策。

3. **规范文案被直接复制而非复用**：为调整两个路径示例，直接把权威 skill 文案拷贝进 Lumen 专属目录，形成了内容分叉。这会导致后续权威文案更新时 Lumen 副本悄然失步，是典型的维护债务，且掩盖了“这不是真正复用的实现”这一事实。

4. **`beforeCondense` 能力缺口**：Lumen 只能观察/记录，无法像现有实现那样在压缩后注入恢复上下文。功能上是纯观测替代，不应计入“对等”。

5. **批处理近似实现未经验证**：Lumen 无原生批完成事件，当前通过复制最相近实现并“择机 flush”来模拟，正确性未经证实，仅是工程近似，不应算作绿色。

### 有序行动项

1. 立即在 `compatibility-matrix.md` 中把上述三项（stop、beforeCondense、batch-completion）从“绿色”改标为“部分/缺口”，不允许自动化指标覆盖人工语义判定。
2. 裁定 compatibility-matrix.md 与 lumen.md 的权威版本，修正矛盾方，明确“published”是否属实。
3. 撤回复制进 Lumen 专属目录的规范文案，改为对权威源做参数化引用，避免内容分叉。
4. 针对 `stop` 缺口给出补偿方案（例如显式在依赖硬阻塞语义的工作流中禁用/降级 Lumen），而非仅记录文档说明。
5. 鉴于距分支关闭仅 35 分钟，上述工作无法负责任地在窗口内完成——建议将 Lumen 适配器整体推迟到下一发布窗口，或仅合并 25 个已验证真正对等的表面，其余三项以 feature flag 显式屏蔽后再评估。
