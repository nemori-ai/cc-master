---
'@ccm/engine': minor
'ccm': minor
---

Add a public RuntimeEnvironment and PathResolver contract for deterministic Linux and macOS home, host-config, plugin-root, session-pointer, and executable resolution, and align CLI discovery and runtime consumers with that single portable path policy.
