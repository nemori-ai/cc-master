---
'ccm': patch
---

Make macOS monitor uninstall fail with a nonzero, replayable result when LaunchAgent unit removal fails after launchd deactivation, and bind live qualification to exact structured `launchctl bootout` evidence.
