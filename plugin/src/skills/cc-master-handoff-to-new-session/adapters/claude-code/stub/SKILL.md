---
name: cc-master-handoff-to-new-session
description: 'Triggers: 当你在 Claude Code 收到 `/cc-master:handoff-to-new-session ...` 或同名命令时，不要直接触发这个 skill；Do NOT treat this as可执行入口，当前条目仅是兼容占位。'
---

# Unsupported Stub

该 skill 为兼容性占位，不作为 Claude Code 的独立入口。

在 Claude Code 下，请使用对应 slash command（/cc-master:handoff-to-new-session）触发该能力。
