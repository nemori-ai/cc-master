---
name: cc-master-view
description: 'Unsupported stub. Triggers: 当你在 Claude Code 收到旧 `$cc-master-view` / `/cc-master:view` 相关请求时，提示正式 viewer 入口已迁到 `ccm web-viewer open`；Do NOT 启动旧 viewer 脚本或维护旧 lifecycle，因为 Claude Code 下该 skill 只是不执行工作的兼容占位。'
---

# Unsupported Stub

该 skill 为兼容性占位，不作为 Claude Code 的独立入口。旧 `/cc-master:view` command 也只保留迁移提示。

请告诉用户使用：

```bash
ccm web-viewer open
```
