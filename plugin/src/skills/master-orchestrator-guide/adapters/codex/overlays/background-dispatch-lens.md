先选 target harness（origin harness 只是候选之一），再按当前 Codex surface 已感知能力选择本 host 的 subagent / 后台 terminal / cloud / external / automation，或用 `ccm` 管理其他 harness worker；API 会话里 subagent 工具可能是 deferred，先 `tool_search` 再派发；不要调用 Claude Code 的 workflow / background-runner 原语；需要机制细节
