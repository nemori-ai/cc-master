先选 target harness（origin harness 只是候选之一），再按当前 Codex surface 已感知能力选择本 host 的 subagent / 后台 terminal / cloud / external / automation，或用 `ccm` 管理其他 harness worker；API 会话里 subagent 工具可能是 deferred，先发现再派发；workflow planning / runtime 边界只按 `worker-routing.md`；需要机制细节
