> 备号数据 = **只读** `${CC_MASTER_HOME:-$HOME/.cc_master}/accounts.json` registry 的生命周期快照（每号取 `last_observed_quota`/`last_switch_out`/`switch_history[]` 里 `at` 最大那条）——usage **绝不写 registry、绝不碰 token**（registry 写/管归 ccm `account` 引擎·概念见 account-pool.md）。当前号 5h/7d 用量按 Claude Code status-line sidecar 取信号（`${CC_MASTER_RATE_CACHE:-${CC_MASTER_HOME:-$HOME/.cc_master}/.cc-master-rate-limits.json}`·ccm 自带的 `ccm statusline` 自动写）。缺则 `available:false` 降级。
