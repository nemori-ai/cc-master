Use when you (orchestrator/agent) read or mutate a cc-master board through the ccm CLI in Codex —— 当你在 Codex 下要用 ccm 操作 board：建板、加改任务、推进状态、查 DAG、记 judgment_call、收 cadence、arm watchdog、查 harness inventory、读当前账号 7d / rolling-24h、查询 provider facts 或刷新 Codex plugin 时。它一体两面：面1是命令与写入关卡；面2是 board 模型与字段取值指南。Triggers: 敲 ccm task/board/jc/cadence/log/watchdog/harness/provider/usage/upgrade、查询模型 facts、当前账号 7d 与过去 24h 消耗、任何 board 写操作、ccm exit 3 / illegal transition / load-bearing 字段被拒。Do NOT use when 你在决定该编排什么、怎么拆 DAG、何时减速或停派（归 master-orchestrator-guide），或解释 usage/estimate verdict（归 pacing-and-estimation）。Codex 只消费当前账号 7d hard ceiling，rolling-24h 仅作 advisory；自动换号永久禁止，`ccm account switch` 与 Claude statusline 机制不可用。
