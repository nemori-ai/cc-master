默认枚举本机已安装且支持 `pluginDistribution` 的 harness，并逐个执行各自 adapter 升级。kimi-code adapter 会从本机已安装的 cc-master 包读取 adapter 产物，刷新本地 kimi-code marketplace/plugin 注册，并按 `plugin/dist/kimi-code` 中的 skills + hooks 进行更新；入口命令以 kimi-code slash commands（`cc-master:as-master-orchestrator` 等）为主。需要只升 kimi-code 时传 `--harness kimi-code`；`--all-harnesses` 现为默认行为的兼容别名（与 `--harness` 互斥）。kimi-code adapter 不触碰 Claude Code settings / marketplace。
