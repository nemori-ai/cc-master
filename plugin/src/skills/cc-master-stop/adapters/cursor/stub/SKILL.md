---
name: cc-master-stop
description: 'Triggers: 当你在 Cursor 收到 `/cc-master-stop ...`（或该 skill 的同名命令入口）时，不能直接触发该 skill；这是 unsupported stub（不应可执行），Do NOT direct-trigger。'
---

# Unsupported Stub

该 skill 为兼容性占位，不作为 Cursor 的独立入口。

在 Cursor 下，请使用对应 slash command（/cc-master-stop）触发该能力。
