# 模型档位 —— 相对成本 + 按难度选档 + 为何主线不切

> **何时读：** 给每个节点选模型档位、纠结升档还是降档、或想清楚为何主线固定一个模型时。这里装的是**事实映射**（哪几档、相对成本、按难度落点）——「把强档集中到临界链上」的**判断**归 `master-orchestrator-guide`「目标即依赖图」镜头，本文不复述决策。

{{PACING_MODEL_TIERS_REFERENCE}}
