Use when 你（orchestrator/agent）在 Cursor IDE Agent 下要消费 ccm 的只读 advisory 把它配速或估算——把长跑对照 5h/7d 配额窗口 pace、估目标 ETA / 查进度偏差(EVM) / 看综合风险 / 算 cost-to-complete、或要读懂 `ccm usage advise` `ccm estimate forecast` 的 verdict 与字段时。它给的是**消费机制知识**：单侧走廊 verdict(hold/throttle/switch/stop_5h/stop_7d)怎么读、估算诚实字段(coverage_pct/confidence/conformal 区间)什么时候该降低对预测的信任、usage 与 estimate 张力怎么看。Triggers: 在 Cursor 下读 ccm usage/estimate/baseline 的输出、"降 WIP 信号出来了怎么落 lever / 该等 reset 还是问用户 / 这个 forecast 信不信 / EVM 偏差怎么看"、配额逼顶要判读 usage 的那一半、pacing/估算 hook 注入提示。Do NOT use when 你要的是**决策**——该不该减速/停派/replan、7d 总闸要不要 surface 用户、范围/期限/配额取舍谁拍板（那是 master-orchestrator-guide）；或 ccm 命令**怎么敲**、board 字段填什么（那是 using-ccm）。Cursor 下 Claude statusline sidecar、号池切号、PostToolBatch 中途采样当前不可用或仅 Stop 侧降级——见 `design_docs/harnesses/capabilities/ccm-quota-account.md` 与 `usage-pacing-midflight.md`；不要假装有 Claude Code 账户权威信号。ccm 出 verdict、决策归 master-orchestrator-guide——本 skill 只教消费层，不替编排做判断。
