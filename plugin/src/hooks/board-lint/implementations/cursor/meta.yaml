hook: board-lint
host: cursor
version: 1
event: postToolUse
matcher: Write
projection:
  target: hooks/
status: implemented
notes:
  - Phase C: ccm board lint advisory after Write to own active board.
  - Envelope: additional_context via launcher (kind:context); never blocks.
