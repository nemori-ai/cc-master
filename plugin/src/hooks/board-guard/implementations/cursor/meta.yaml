hook: board-guard
host: cursor
version: 1
event: preToolUse
matcher: Shell|Write
projection:
  target: hooks/
status: implemented
notes:
  - Phase C: deny hand-edits to active board (Write path + Shell write heuristics).
  - Cursor tool surface has no Edit/MultiEdit/apply_patch/Bash — Shell replaces Bash.
  - Envelope: permission:deny via launcher (kind:block).
