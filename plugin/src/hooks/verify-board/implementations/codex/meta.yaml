hook: verify-board
host: codex
event: Stop
mode: blocking-continuation-gate
notes:
  - Emits Codex Stop decision:block when active boards still need continuation.
  - Uses board runtime.stop_allow_until as the short-lived release valve after independent self-check.
  - UserPromptSubmit resets the release valve for the current session board before processing new input.
