hook: verify-board
host: cursor
version: 1
event: stop
matcher: ""
projection:
  target: hooks/
status: implemented-followup
notes:
  - Phase C: Stop gate notes + FUSE=5 + stop_allow_until release valve.
  - Cursor has no decision:block on Stop — launcher maps kind:block → followup_message (D6).
  - loop_limit:5 in hooks.json aligns with app FUSE.
