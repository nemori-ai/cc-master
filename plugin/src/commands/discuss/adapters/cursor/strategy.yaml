host: cursor
command: discuss
version: 1
mode: host_native
projection:
  source: body.md
  target: commands/discuss.md
notes:
  - Cursor plugin commands map to /discuss in Agent chat (no plugin: namespace).
  - Body adapted from Claude Code host_native; path tokens removed; Shell not Bash.
