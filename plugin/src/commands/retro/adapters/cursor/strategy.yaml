host: cursor
command: retro
version: 1
mode: host_native
projection:
  source: body.md
  target: commands/retro.md
notes:
  - Cursor plugin commands map to /retro in Agent chat (no plugin: namespace).
  - Body adapted from Claude Code host_native; path tokens removed; Shell not Bash.
