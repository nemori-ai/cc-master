host: cursor
command: handoff-to-new-session
version: 1
mode: host_native
projection:
  source: body.md
  target: commands/handoff-to-new-session.md
notes:
  - Cursor plugin commands map to /handoff-to-new-session in Agent chat (no plugin: namespace).
  - Body adapted from Claude Code host_native; path tokens removed; Shell not Bash.
