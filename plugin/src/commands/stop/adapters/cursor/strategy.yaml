host: cursor
command: stop
version: 1
mode: host_native
projection:
  source: body.md
  target: commands/cc-master-stop.md
notes:
  - Cursor plugin commands map to /cc-master-stop in Agent chat (no plugin: namespace).
  - Body adapted from Claude Code host_native; path tokens removed; Shell not Bash.
  - Target filename/frontmatter name is cc-master-stop to avoid Cursor built-in /stop.
