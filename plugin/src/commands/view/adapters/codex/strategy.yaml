host: codex
command: view
version: 1
mode: adapter_guidance
surface:
  kind: user_prompt_skill_script_guided
  user_prompt: plugin/src/skills/cc-master-view/canonical/SKILL.md
  summary: "Run the view server bundled with master-orchestrator-guide after resolving the skill directory."
  command_template: "CC_MASTER_BOARD=\"<board-path>\" node \"<master-orchestrator-guide-skill-dir>/scripts/view-server.js\""
notes:
  - Codex 目前的实现是 skill-first：无 native `/cc-master:view` slash command，viewer 入口通过 `plugin/src/skills/cc-master-view/canonical/SKILL.md` 和 `view-server` 脚本协同。
  - Codex has no verified plugin root env token equivalent to CLAUDE_PLUGIN_ROOT.
  - The script is distributed in the Codex skill bundle; do not use Claude path tokens in Codex guidance.
