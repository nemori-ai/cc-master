host: codex
command: view
version: 1
mode: adapter_guidance
surface:
  kind: deprecated_shim
  user_prompt: plugin/src/skills/cc-master-view/canonical/SKILL.md
  summary: "Deprecated: tell users to run `ccm web-viewer open`; do not launch legacy viewer scripts."
  command_template: "ccm web-viewer open"
notes:
  - Codex 旧入口 `$cc-master-view` 只保留迁移提示；正式 viewer 生命周期由 ccm `web-viewer` namespace 管理。
  - Codex has no verified plugin root env token equivalent to CLAUDE_PLUGIN_ROOT.
  - Do not use Claude path tokens or direct skill script paths in Codex guidance.
