---
point: sdd.implementation-gate
---

## 权威陈述

<!-- ccm:k:start point:sdd.implementation-gate -->
## 硬闸：动手写第一行实现前

```
命中「值得引入 SDD」的场景 且 此刻无已认可 spec → 停下，先产出/先要 spec，不写产码。
```

这不是建议，是前置条件——与 TDD 的铁律「没有失败的测试，不写产码」同构：这里是「没有已认可的 spec，不写产码」。

判定两步，动手前都要过一遍：

1. **这个改动命中上表哪一行？** 跨边界合约 / 多方消费者 / 接口稳定性要求高 / 并行开发 / 领域复杂不变式密集——命中任一即"值得"，进第 2 步；一条都不占，直接实现，不必等 spec。
2. **若值得，此刻是否已有一份先于实现、被消费方认可的 spec？** 哪怕只是几行接口签名 + 错误契约 + 关键不变式列表也算数。没有 → 先写出它（或先请求一轮 scoping / 向消费方确认关键决策点），再动手写实现代码。

**"acceptance 已经写得很清楚了"不能替代这一步。** acceptance 锁的是外部可观察行为，锁不住内部决策——存储选型、并发/一致性语义、失败模式（fail-open 还是 fail-closed）、状态在重启和多副本之间怎么处置、错误响应的 schema。这些决策若不显式写下来，就会在压力下被默认、被遗忘，又被下一个读者当成"已经想清楚的设计"照单全收。

**时间紧、无人可评审，都不构成跳过的理由——那正是最需要显式化的时刻。** 没有评审者时，spec 就是你唯一能拿来对照检查自己决策的外部标准；跳过它不是省了一步文档，是把一堆本该显式的决定悄悄留给了自己的默认值。

---

<!-- ccm:k:end point:sdd.implementation-gate -->

## 失效类型

`motivation_conflict`（主体：行为约束） —— 删掉后,agent 会在压力下为『这次不需要 spec,我已经想清楚了』构造出听起来严密的技术理由,直接写实现代码,跳过先固定关键决策这一步。

主体是「无已认可 spec 不写产码」的硬闸，停下来先产出 spec 比直接开写慢得多，属于选择不做。

## 边界

触发点是『即将写下第一行实现代码』这一刻,不是需求收集阶段,也不是代码写完后回头补;每次新的、独立命中『值得』标准的改动都要重新过闸,不是项目早前写过一次 spec 就一劳永逸。

## 为什么它随模型变强而更重要

模型越强,越擅长现场构造一份听起来严密的理由——『接口/并发/失败模式我已经在脑子里想清楚了,直接写就是最优路径』——这份论证越具体越像技术判断,越容易被当成合法豁免,而不是被当成正在跳过硬闸的信号。

## 失败形态

代码写完质量不错,接口清晰、测试补齐,描述里写着『设计经过充分考虑』——但从头到尾没有一份先于实现、被消费方认可过的文本存在过,所谓『考虑过』只存在于代码和作者脑子里,没有可供事先审阅的产物。
