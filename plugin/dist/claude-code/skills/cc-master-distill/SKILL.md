---
name: cc-master-distill
description: 'Triggers: 当你在 Claude Code 收到 `/cc-master:distill ...`（或该 skill 的同名命令入口）时，不能直接触发该 skill；Do NOT execute directly，这只是 unsupported stub 说明位。'
---

# Unsupported Stub

该 skill 为兼容性占位，不作为 Claude Code 的独立入口。

在 Claude Code 下，请使用对应 slash command（/cc-master:distill）触发该能力。
