---
name: cc-master-retro
description: 'Triggers: 当你在 kimi-code 收到 `cc-master:retro ...`（或该 skill 的同名命令入口）时，不能直接触发该 skill；这是 unsupported stub（不应可执行），Do NOT direct-trigger。'
---

# Unsupported Stub

该 skill 为兼容性占位，不作为 kimi-code 的独立入口。

在 kimi-code 下，请使用对应 plugin 命令 `cc-master:retro` 触发该能力。
