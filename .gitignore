# test scratch (hook tests create temp project dirs)
.tmp-*/
tests/**/.tmp-*/
# controlled attestation / updater staging only (unknown .tmp content stays visible)
/.tmp/ccm-provider-guidance-*/
/.tmp/ccm-pacing-read-only-*/
# whole-host sync staging siblings under plugin/dist (K1-06 v9; bak stays visible)
/plugin/dist/*.write-*/
# OS / editor
.DS_Store
*.swp
# never commit a real runtime board into the plugin repo
.claude/cc-master/
# working plans + build board — not version-controlled
design_docs/plans/
# local skill-knowledge begin/validate/apply transaction workspaces
.skill-knowledge/workspaces/
.gstack/
# ccm monorepo build artifacts (T0 industrial stack)
ccm/node_modules
ccm/**/node_modules
ccm/**/dist
ccm/**/.native-build
ccm/.turbo
ccm/**/.turbo
ccm/**/*.tsbuildinfo
# build cache (turborepo)
.turbo/
# package-plugin.sh 产物（plugin 分发 zip，构建物不入版本控制）
/dist/
